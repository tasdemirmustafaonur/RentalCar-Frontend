import { FilterCarModelPipePipe } from './filter-car-model-pipe.pipe';

describe('FilterCarModelPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarModelPipePipe();
    expect(pipe).toBeTruthy();
  });
});
